@model  QlikPlatformManager.ViewModels.ArchiverStructureViewModel
@{

}

<script type="text/javascript">
    $.validator.unobtrusive.parse(document);
</script>

<div class="container-fluid" id="ContentDiv">

    @*ZONE ARIANE*@
    <div class="row">
        <div class="col-sm-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item active">Archivage</li>
                    <li class="breadcrumb-item active" aria-current="page">Stucture</li>
                </ol>
            </nav>
            <br />
        </div>
    </div>

    @*ZONE POST FORMULAIRE*@
    <div class="row">
        <div class="col-md-12">
            @*@using (Html.BeginForm())*@
            @using (Ajax.BeginForm("Structure", "Archiver",  null, new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "ContentDiv", OnBegin = "onBegin", OnComplete = "onComplete", OnFailure = "Erreur" }, new { @id = "ServeurRef" }))
            {
                @*ZONE SAISIE*@
                <div class="row">
                    @*--------Colonne de gauche---------*@
                    <div class="col-md-5">
                        <fieldset class="form-group">
                            <legend>Application de référence</legend>
                            <br />
                            @*@{
                        string name = Html.NameFor(m => m.ServeurRef).ToString();
                        string prefix = Html.ViewContext.ViewData.TemplateInfo.GetFullHtmlFieldName(name);
                        ViewDataDictionary viewData = new ViewDataDictionary(Html.ViewData)
                        {
                            TemplateInfo = new TemplateInfo { HtmlFieldPrefix = prefix }
                        };
                    }
                    @Html.Partial("ConnexionServeur", Model.ServeurRef, viewData)*@

                            @*Appel vue partielle*@
                            @Html.Partial("StructureConnexion", Model.ServeurRef, Model.GetViewData(Html, "ServeurRef"))
                            @*@Html.Partial("StructureConnexion", Model.ServeurRef)*@

                            <br />
                        </fieldset>
                    </div>
                    @*--------Colonne de séparation---------*@
                    <div class="col-md-1">
                    </div>
                    @*--------Colonne de droite---------*@
                    <div class="col-md-5">
                        <fieldset class="form-group">
                            <legend>Application de comparaison</legend>
                            <br />
                            @*@Html.Partial("StructureConnexion", Model.ServeurRef, Model.GetViewData(Html, "ServeurRef"))*@
                            <br />
                        </fieldset>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        <div class="form-group row">
                            <div class="col-md-2">
                                <button id="ActionStateBtn" type="submit" class="btn btn-outline-success btn-sm btn-lg btn-block">
                                    @*disabled*@
                                    <span id="ActionStateSpr" class="spinner-border spinner-border-sm" role="status" aria-hidden="true" style="display:none"></span>
                                    Comparer
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                @*ZONE RESULTATS*@
                <div class="row">
                    <div id="ResultsDiv" class="col-md-12">
                        @if (!String.IsNullOrEmpty(Model.Results.Title))
                        {
                            @Html.Partial("Results", Model.Results)
                        }
                    </div>
                </div>
            }
        </div>
    </div>
</div>
